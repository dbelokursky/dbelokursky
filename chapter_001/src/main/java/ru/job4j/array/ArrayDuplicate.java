package ru.job4j.array;

/**
* Класс ArrayDuplicate для удаление дубликатов из массива строк.
* @author Dmitry Belokursky
* @since 0.1 12.06.2017
*/
public class ArrayDuplicate {
	// public String[] remove(String[] array) {
	// 	int counter = 0;
	// 	int lastElementInd = array.length - 1;
	// 	for (int i = 0; i < lastElementInd; i++) {
	// 		int nextElementInd = i + 1;
	// 		if (array[i].equals(array[nextElementInd])) {
	// 			if ((nextElementInd) != lastElementInd {
	// 				int tmp = array[lastElementInd];
	// 				array[lastElementInd] = array[nextElementInd];
	// 				array[nextElementInd] = tmp;
	// 				counter++;
	// 			} else {
	// 				int tmp = array[lastElementInd - counter];
	// 				array[lastElementInd - counter] = array[nextElementInd];
	// 				array[nextElementInd] = tmp;
	// 				counter++;
	// 			}
	// 		}
	// 	}
	// 	return Arrays.copyOf(array, array.length - counter);
	// }
}